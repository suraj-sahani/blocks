1. Make sure client and server search params are in sync.
2. Fix Check-in and Check-out time picker for user
3. When user sign's up, provide an input to upload their profile picture,
   also update better-auth's default user shema to include phone number at the time of signing up.

Updates for Add Parking Area Form:
Make the form a multi-step form with beautiful UI and smooth animations.
The 1st step will require the host to add the following fields:
Location Name, Location Description, Location Address, Latitude, Longitude, State, City and Zip Code and images.

The 2nd step will require the host to add the following fields:
Total Parking Capacity, Amenities, Schedule for the opening and closing time.

The 3rd step will contain a UI for individually selecting each parking slot such that the host can specify the vehicle type for each slot and the price.

Updates for Add EV Station Form:
Follow the same steps as Add Parking Area Form but update the fields to be for an EV Station.
